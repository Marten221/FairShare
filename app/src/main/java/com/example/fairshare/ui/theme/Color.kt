package com.example.fairshare.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color definitions for the FairShare application theme.
 *
 * These colors are used to build light and dark color schemes
 * following Material Design guidelines.
 */

/** Primary purple color for dark theme (lighter variant). */
val Purple80 = Color(0xFFD0BCFF)

/** Secondary purple-grey color for dark theme (lighter variant). */
val PurpleGrey80 = Color(0xFFCCC2DC)

/** Tertiary pink color for dark theme (lighter variant). */
val Pink80 = Color(0xFFEFB8C8)

/** Primary purple color for light theme (darker variant). */
val Purple40 = Color(0xFF6650a4)

/** Secondary purple-grey color for light theme (darker variant). */
val PurpleGrey40 = Color(0xFF625b71)

/** Tertiary pink color for light theme (darker variant). */
val Pink40 = Color(0xFF7D5260)